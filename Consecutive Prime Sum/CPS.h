//file		:	CPS.h
//author	:	steaKK

#ifndef CPS_H
#define CPS_H

#include <iostream>

using namepspace std;

class CPS {
public:
	static long solve(long);
	static long highest(long);
	static bool is_prime(long);
};

#endif
