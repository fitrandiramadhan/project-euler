//file		:	ReversibleNumber.hpp
//author	:	steaKK

#ifndef ReversibleNumber_HPP
#define ReversibleNumber_HPP

#include <iostream>
#include <string>

using namespace std;

class ReversibleNumber {
public:
	static long reverse(long);
	static bool is_odd(long);
	static long solve(long);
};

#endif
