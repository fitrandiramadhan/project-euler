//file		:	SquareDigitChain.hpp
//author	:	steaKK

#ifndef SquareDigitChain_HPP
#define SquareDigitChain_HPP

#include <iostream>

using namespace std;

class SquareDigitChain {
public:
	static bool is_done(long);
	static bool is_89(long);
	static long run_once(long);
	static bool run_through(long);
	static long solve(long);
};

#endif
